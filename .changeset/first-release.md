---
'single-flight-auth': minor
---

First release: single-flight refresh within a tab, Web Locks (or a localStorage lease) across tabs,
BroadcastChannel sync, `fetch` with a single 401 retry, and session-lost handling.
