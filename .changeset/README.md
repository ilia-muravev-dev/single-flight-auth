# Changesets

Release notes live here as small markdown files, one per change. Run `pnpm changeset` to add one;
the release workflow turns them into a version bump, a CHANGELOG entry and an npm publish.
